function order(item) {
  const discordUser = "rattyray1";
  const invite = "https://discord.gg/YOUR_INVITE"; // replace with your invite
  alert(`Order for ${item}!\n\nMessage Discord user: ${discordUser}\nOr join: ${invite}`);
}
